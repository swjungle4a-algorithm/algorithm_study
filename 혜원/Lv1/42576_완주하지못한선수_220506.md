```python

def solution(participant, completion):
    answer = ''
    participant.sort()
    completion.sort()
    
    idx = 0
    for p, c in zip(participant, completion):
        if p!=c: 
            answer = p
            break
        idx += 1
    else:
        answer = participant[idx]
        
    return answer

```
